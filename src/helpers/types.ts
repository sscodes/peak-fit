export enum AUTH_FORMAT {
  SIGN_UP = 'up',
  SIGN_IN = 'in',
}

export enum NETWORK_STATUS {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum BUTTON_VARIANT {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  DANGER = 'danger',
  WARNING = 'warning',
  INFO = 'info',
}

export enum BUTTON_TYPE {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}
