export enum AUTH_FORMAT {
  SIGN_UP = 'up',
  SIGN_IN = 'in',
}

export enum NETWORK_STATUS {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum BUTTON_VARIANT {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  DANGER = 'danger',
  WARNING = 'warning',
  INFO = 'info',
}

export enum BUTTON_TYPE {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

export enum BUTTON_SIZE {
  LARGE = 'large',
  REGULAR = 'regular',
  SMALL = 'small',
}

export enum STEPPER_1_PROGRESS {
  LINE = 'line',
  DOTS = 'dots',
}
