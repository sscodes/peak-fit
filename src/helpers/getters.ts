export const HOME = '/dashboard';

export const AUTH_HOME = '/auth';
export const SIGN_IN = `${AUTH_HOME}/sign-in`;
export const SIGN_UP = `${AUTH_HOME}/sign-up`;
export const VERIFY_EMAIL = `${AUTH_HOME}/verify-email`;

export const FORGOT_PASSWORD = `${AUTH_HOME}/forgot-password`;
